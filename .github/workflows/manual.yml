name: my_manual_workflow

on:
  workflow_dispatch:

jobs:
  first-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: |
          echo "Cadana"
          echo "Torono"
  
